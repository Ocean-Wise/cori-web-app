import styled from 'styled-components';

const Title = styled.span`
  color: #FFFFFF;
  font-size: 28px;
  font-weight: 300;
  letter-spacing: 3px;
  line-height: 35px;
  text-align: center;
  width: inherit;
  position: absolute;
  bottom: 16px;
`;

export default Title;
