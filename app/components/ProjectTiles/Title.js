import styled from 'styled-components';

const Title = styled.span`
  color: #FFFFFF;
  font-size: 28px;
  font-weight: bold;
  line-height: 35px;
  text-align: center;
  position: absolute;
  bottom: 10px;
  left: 60px;
`;

export default Title;
