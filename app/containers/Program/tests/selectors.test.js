// import { fromJS } from 'immutable';
// import { selectProgramDomain } from '../selectors';

describe('selectProgramDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
