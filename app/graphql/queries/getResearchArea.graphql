#import "../fragments/researchAreaTeaser.graphql"
#import "../fragments/programTeaser.graphql"
#import "../fragments/projectTeaser.graphql"

query ResearchArea($area: String!) {
  researchAreas(q: $area) {
    ...ResearchAreaTeaser
    subheader
    copy
    newsRSS
    mediaRSS
    teamMembers {
      first
      last
      honorifictitle
      image {
        title
        url
      }
    }
    programs {
      ...ProgramTeaser
    }
    projects {
      ...ProjectTeaser
    }
  }
}
