query Publications($limit: Int, $skip: Int, $sort: String) {
  publications(skip: $skip, limit: $limit, q: $sort) {
    slug
    citation
    title
    abstract
    authors
    year
    keywords
    doi
    pdf {
      url
    }
    url
    researchArea {
      slug
    }
  }
}
