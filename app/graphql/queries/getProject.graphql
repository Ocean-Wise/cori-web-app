#import "../fragments/projectTeaser.graphql"

query Project($slug: String!) {
  projects(q: $slug) {
    ...ProjectTeaser
    layout {
      contentModules {
        sys {
          id
          contentTypeId
        }
      }
    }
  }
}
